# social-media-service

## How to run this project
  - Windows:
    - `docker-compose up`
    - `gradlew clean build`
    - `gradlew bootRun`
  - Linux:
    - `sudo docker-compose up`
    - `./gradlew clean build`
    - `./gradlew bootRun`
